# 🎤 음성 녹음 기능 설정 가이드 (팀원용)

이 가이드는 음성 녹음 기능을 처음 사용하는 팀원을 위한 **간단한 설정 가이드**입니다.

---

## ⚡ 빠른 시작 (3단계)

### 1️⃣ Google Cloud 설정 (최초 1회만)

#### Google Cloud 프로젝트 만들기
1. [Google Cloud Console](https://console.cloud.google.com/) 접속
2. 상단 프로젝트 선택 → **"새 프로젝트"** 클릭
3. 프로젝트 이름 입력 (예: `calmdesk-voice`)
4. **프로젝트 ID 기록** (예: `my-project-111-487708`)

#### Speech-to-Text API 활성화
1. "API 및 서비스" → "라이브러리" 이동
2. 검색: **"Cloud Speech-to-Text API"**
3. **"사용 설정"** 클릭

#### 서비스 계정 키 파일 다운로드
1. "IAM 및 관리자" → "서비스 계정" 이동
2. **"서비스 계정 만들기"** 클릭
3. 서비스 계정 이름: `speech-to-text-service`
4. 역할: **"Cloud Speech-to-Text API 사용자"** 선택
5. "키" 탭 → "키 추가" → **"JSON"** 선택
6. **JSON 파일 다운로드** → 안전한 곳에 저장
   - 예: `C:\google-speech-api\my-project-111-487708-32588b2dbf85.json`

---

### 2️⃣ 백엔드 설정

#### 방법 A: 자동 스크립트 사용 (추천)

**Windows:**
```powershell
cd "파이널 백엔드"
.\setup-voice-recording.ps1
```

**Linux/Mac:**
```bash
cd "파이널 백엔드"
chmod +x setup-voice-recording.sh
./setup-voice-recording.sh
```

스크립트가 안내하는 대로 입력:
- 프로젝트 ID
- 서비스 계정 키 파일 경로

#### 방법 B: 수동 설정

`파이널 백엔드/src/main/resources/application-secret.yaml` 파일을 열고 다음을 추가/수정:

```yaml
app:
  call-record:
    google-cloud:
      project-id: 여기에_프로젝트_ID_입력
      credentials-path: 여기에_키_파일_절대경로_입력
```

**예시 (Windows):**
```yaml
app:
  call-record:
    google-cloud:
      project-id: my-project-111-487708
      credentials-path: C:\google-speech-api\my-project-111-487708-32588b2dbf85.json
```

**예시 (Linux/Mac):**
```yaml
app:
  call-record:
    google-cloud:
      project-id: my-project-111-487708
      credentials-path: /home/user/google-speech-api/my-project-111-487708-32588b2dbf85.json
```

---

### 3️⃣ 서버 재시작 및 테스트

1. **백엔드 서버 재시작**
   ```bash
   # Windows
   .\gradlew bootRun
   
   # Linux/Mac
   ./gradlew bootRun
   ```

2. **로그 확인**
   서버 시작 시 다음 메시지가 보이면 성공:
   ```
   Google Cloud 인증: SpeechClient 생성 완료
   ```

3. **프론트엔드에서 테스트**
   - `/app/call` 페이지로 이동
   - "녹음 시작" 버튼 클릭
   - 마이크 권한 허용
   - 음성 녹음 후 업로드
   - 통화 기록에서 텍스트 변환 결과 확인

---

## ❓ 문제 해결

### 오류: "인증 JSON 파일을 찾을 수 없습니다"
- ✅ `credentials-path`가 올바른지 확인
- ✅ **절대 경로** 사용 (상대 경로 X)
- ✅ 파일 경로에 한글이나 특수문자 없도록

### 오류: "API not enabled"
- ✅ Google Cloud Console에서 Speech-to-Text API 활성화 확인
- ✅ 서비스 계정에 "Cloud Speech-to-Text API 사용자" 역할 확인

### 오류: "Project not found"
- ✅ `project-id`가 정확한지 확인
- ✅ Google Cloud Console에서 프로젝트 ID 재확인

---

## 💡 팁

- **무료 할당량**: 월 60분 (매월 초기화)
- **서비스 계정 키 파일은 절대 Git에 올리지 마세요!**
- `application-secret.yaml`은 이미 `.gitignore`에 포함되어 있습니다

---

## 📚 더 자세한 가이드

자세한 설명이 필요하면 `VOICE_RECORDING_SETUP_GUIDE.md` 파일을 참고하세요.

---

## 🆘 도움이 필요하면

1. 이 가이드의 "문제 해결" 섹션 확인
2. 백엔드 로그 확인
3. 팀 리더에게 문의

